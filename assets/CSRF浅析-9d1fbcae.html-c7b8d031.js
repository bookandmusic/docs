import{_ as e}from"./plugin-vue_export-helper-c27b6911.js";import{o as m,c as o,e as r}from"./app-76561525.js";const t={},a=r('<p><em>CSRF</em>跨站点请求伪造(<em>Cross—Site Request Forgery</em>)，存在巨大的危害性，你可以这样来理解：</p><p>​ 攻击者盗用了你的身份，以你的名义发送恶意请求，对服务器来说这个请求是完全合法的，但是却完成了攻击者所期望的一个操作，比如以你的名义发送邮件、发消息，盗取你的账号，添加系统管理员，甚至于购买商品、虚拟货币转账等。如下：其中<em>Web A</em>为存在<em>CSRF</em>漏洞的网站，<em>Web B</em>为攻击者构建的恶意网站，<em>User C</em>为<em>Web A</em>网站的合法用户。</p><h1 id="csrf攻击攻击原理" tabindex="-1"><a class="header-anchor" href="#csrf攻击攻击原理" aria-hidden="true">#</a> <em>CSRF</em>攻击攻击原理</h1><ol><li><p>用户C打开浏览器，访问受信任网站A，输入用户名和密码请求登录网站A；</p></li><li><p>在用户信息通过验证后，网站A产生<em>Cookie</em>信息并返回给浏览器，此时用户登录网站A成功，可以正常发送请求到网站A；</p></li><li><p>用户未退出网站A之前，在同一浏览器中，打开一个<em>TAB</em>页访问网站B；</p></li><li><p>网站B接收到用户请求后，返回一些攻击性代码，并发出一个请求要求访问第三方站点A；</p></li><li><p>浏览器在接收到这些攻击性代码后，根据网站B的请求，在用户不知情的情况下携带<em>Cookie</em>信息，向网站A发出请求。网站A并不知道该请求其实是由B发起的，所以会根据用户C的<em>Cookie</em>信息以C的权限处理该请求，导致来自网站B的恶意代码被执行。</p></li></ol><figure><img src="https://img.bookandmusic.cn/blog/2022/01/j4vaAK.jpg" alt="j4vaAK" tabindex="0" loading="lazy"><figcaption>j4vaAK</figcaption></figure><blockquote><p><strong>注意</strong>: 通过<code>HTML</code>标签的<code>src</code>属性发起的请求是不遵循同源策略的。</p></blockquote><p>受害者 Bob 在银行有一笔存款，通过对银行的网站发送请求 <code>http://bank.example/withdraw?account=bob&amp;amount=1000000&amp;for=bob2</code> 可以使 <em>Bob</em> 把 1000000 的存款转到 <em>bob2</em> 的账号下。通常情况下，该请求发送到网站后，服务器会先验证该请求是否来自一个合法的 <em>session</em>，并且该 <em>session</em> 的用户 <em>Bob</em> 已经成功登陆。</p><p>​ 黑客 Mallory 自己在该银行也有账户，他知道上文中的 URL 可以把钱进行转帐操作。Mallory 可以自己发送一个请求给银行：<code>http://bank.example/withdraw?account=bob&amp;amount=1000000&amp;for=Mallory</code>。但是这个请求来自 Mallory 而非 Bob，他不能通过安全认证，因此该请求不会起作用。</p><p>​ 这时，Mallory 想到使用 CSRF 的攻击方式，他先自己做一个网站，在网站中放入如下代码： <code>src=&quot;http://bank.example/withdraw?account=bob&amp;amount=1000000&amp;for=Mallory&quot;</code>，并且通过广告等诱使 <em>Bob</em> 来访问他的网站。</p><p>当 <em>Bob</em> 访问该网站时，上述 <em>url</em> 就会从 <em>Bob</em> 的浏览器发向银行，而这个请求会附带 Bob 浏览器中的 <em>cookie</em> 一起发向银行服务器。</p><p>大多数情况下，该请求会失败，因为他要求 <em>Bob</em> 的认证信息。但是，如果 <em>Bob</em> 当时恰巧刚访问他的银行后不久，他的浏览器与银行网站之间的 <em>session</em> 尚未过期，浏览器的 <em>cookie</em> 之中含有 <em>Bob</em> 的认证信息。这时，悲剧发生了，这个 <em>url</em> 请求就会得到响应，钱将从 <em>Bob</em> 的账号转移到 <em>Mallory</em> 的账号，而 <em>Bob</em> 当时毫不知情。</p><p>等以后 <em>Bob</em> 发现账户钱少了，即使他去银行查询日志，他也只能发现确实有一个来自于他本人的合法请求转移了资金，没有任何被攻击的痕迹。而 <em>Mallory</em> 则可以拿到钱后逍遥法外。</p><h1 id="csrf漏洞检测" tabindex="-1"><a class="header-anchor" href="#csrf漏洞检测" aria-hidden="true">#</a> <em>CSRF</em>漏洞检测</h1><p>​ 检测<em>CSRF</em>漏洞是一项比较繁琐的工作，最简单的方法就是抓取一个正常请求的数据包，去掉Referer字段后再重新提交，如果该提交还有效，那么基本上可以确定存在<em>CSRF</em>漏洞。</p><p>​ 随着对<em>CSRF</em>漏洞研究的不断深入，不断涌现出一些专门针对<em>CSRF</em>漏洞进行检测的工具，如<em>CSRFTester</em>，<em>CSRF Request Builder</em>等。</p><p>​ 以<em>CSRFTester</em>工具为例，CSRF漏洞检测工具的测试原理如下：使用<em>CSRFTester</em>进行测试时，首先需要抓取我们在浏览器中访问过的所有链接以及所有的表单等信息，然后通过在<em>CSRFTester</em>中修改相应的表单等信息，重新提交，这相当于一次伪造客户端请求。如果修改后的测试请求成功被网站服务器接受，则说明存在<em>CSRF</em>漏洞，当然此款工具也可以被用来进行<em>CSRF</em>攻击。</p><h1 id="防御csrf攻击" tabindex="-1"><a class="header-anchor" href="#防御csrf攻击" aria-hidden="true">#</a> 防御CSRF攻击</h1><p>​ 目前防御 CSRF 攻击主要有三种策略：验证 <em>HTTP Referer</em> 字段；在请求中添加 <em>token</em> 并验证；在 <em>HTTP</em> 头中自定义属性并验证。</p><h2 id="验证-http-referer-字段" tabindex="-1"><a class="header-anchor" href="#验证-http-referer-字段" aria-hidden="true">#</a> 验证 <em>HTTP Referer</em> 字段</h2><p>​ 根据 <em>HTTP</em> 协议，在 <em>HTTP</em> 头中有一个字段叫 <em>Referer</em>，它记录了该 <em>HTTP</em> 请求的来源地址。在通常情况下，访问一个安全受限页面的请求来自于同一个网站，比如需要访问 <code>http://bank.example/withdraw?account=bob&amp;amount=1000000&amp;for=Mallory</code>，用户必须先登陆 <code>bank.example</code>，然后通过点击页面上的按钮来触发转账事件。这时，该转帐请求的 <em>Referer</em> 值就会是转账按钮所在的页面的 <em>URL</em>，通常是以 <code>bank.example</code> 域名开头的地址。</p><p>而如果黑客要对银行网站实施 <em>CSRF</em> 攻击，他只能在他自己的网站构造请求，当用户通过黑客的网站发送请求到银行时，该请求的 <em>Referer</em> 是指向黑客自己的网站。因此，要防御 <em>CSRF</em> 攻击，银行网站只需要对于每一个转账请求验证其 <em>Referer</em> 值，如果是以 <code>bank.example</code> 开头的域名，则说明该请求是来自银行网站自己的请求，是合法的。如果 <em>Referer</em> 是其他网站的话，则有可能是黑客的 <em>CSRF</em> 攻击，拒绝该请求。</p><p>​ 这种方法的显而易见的好处就是简单易行，网站的普通开发人员不需要操心 <em>CSRF</em> 的漏洞，只需要在最后给所有安全敏感的请求统一增加一个拦截器来检查 <em>Referer</em> 的值就可以。特别是对于当前现有的系统，不需要改变当前系统的任何已有代码和逻辑，没有风险，非常便捷。</p><p>​ 然而，这种方法并非万无一失。<em>Referer</em> 的值是由浏览器提供的，虽然 <em>HTTP</em> 协议上有明确的要求，但是每个浏览器对于 <em>Referer</em> 的具体实现可能有差别，并不能保证浏览器自身没有安全漏洞。使用验证 <em>Referer</em> 值的方法，就是把安全性都依赖于第三方（即浏览器）来保障，从理论上来讲，这样并不安全。</p><p>事实上，对于某些浏览器，比如 IE6 或 FF2，目前已经有一些方法可以篡改 Referer 值。如果 <code>bank.example</code> 网站支持 IE6 浏览器，黑客完全可以把用户浏览器的 <em>Referer</em> 值设为以 <code>bank.example</code> 域名开头的地址，这样就可以通过验证，从而进行 <em>CSRF</em> 攻击。</p><p>即便是使用最新的浏览器，黑客无法篡改 <em>Referer</em> 值，这种方法仍然有问题。因为 <em>Referer</em> 值会记录下用户的访问来源，有些用户认为这样会侵犯到他们自己的隐私权，特别是有些组织担心 <em>Referer</em> 值会把组织内网中的某些信息泄露到外网中。因此，用户自己可以设置浏览器使其在发送请求时不再提供 <em>Referer</em>。当他们正常访问银行网站时，网站会因为请求没有 <em>Referer</em> 值而认为是 <em>CSRF</em> 攻击，拒绝合法用户的访问。</p><h2 id="在请求中添加-token-参数并验证" tabindex="-1"><a class="header-anchor" href="#在请求中添加-token-参数并验证" aria-hidden="true">#</a> 在请求中添加 <em>token</em> 参数并验证</h2><p>​ <em>CSRF</em> 攻击之所以能够成功，是因为黑客可以完全伪造用户的请求，该请求中所有的用户验证信息都是存在于 <em>cookie</em> 中，因此黑客可以在不知道这些验证信息的情况下直接利用用户自己的 <em>cookie</em> 来通过安全验证。</p><blockquote><p><strong>要抵御 <em>CSRF</em>，关键在于在请求中放入黑客所不能伪造的信息，并且该信息不存在于 <em>cookie</em> 之中。可以在 <em>HTTP</em> 请求中以参数的形式加入一个随机产生的 <em>token</em>，并在服务器端建立一个拦截器来验证这个 <em>token</em>，如果请求中没有 <em>token</em> 或者 <em>token</em> 内容不正确，则认为可能是 <em>CSRF</em> 攻击而拒绝该请求。</strong></p></blockquote><p>​ 这种方法要比检查 <em>Referer</em> 要安全一些，<em>token</em> 可以在用户登陆后产生并放于 <em>session</em> 之中，然后在每次请求时把 <em>token</em> 从 <em>session</em> 中拿出，与请求中的 <em>token</em> 进行比对，但这种方法的难点在于如何把 <em>token</em> 以参数的形式加入请求。</p><p>对于 <em>GET</em> 请求，<em>token</em> 将附在请求地址之后，这样 <em>URL</em> 就变成 <code>http://url?csrftoken=tokenvalue</code>。</p><p>而对于 POST 请求来说，要在 <em>form</em> 的最后加上 <code>&lt;input type=&quot;hidden&quot; name=&quot;csrftoken&quot; value=&quot;tokenvalue&quot;/&gt;</code>，这样就把 <em>token</em> 以参数的形式加入请求了。</p><p>但是，在一个网站中，可以接受请求的地方非常多，要对于每一个请求都加上 <em>token</em> 是很麻烦的，并且很容易漏掉，通常使用的方法就是在每次页面加载时，使用 <em>javascript</em> 遍历整个 <em>dom</em> 树，对于 <em>dom</em> 中所有的 <em>a</em> 和 <em>form</em> 标签后加入 <em>token</em>。这样可以解决大部分的请求，但是对于在页面加载之后动态生成的 <em>html</em> 代码，这种方法就没有作用，还需要程序员在编码时手动添加 <em>token</em>。</p><p>​ 该方法还有一个缺点是难以保证 <em>token</em> 本身的安全。特别是在一些论坛之类支持用户自己发表内容的网站，黑客可以在上面发布自己个人网站的地址。由于系统也会在这个地址后面加上 <em>token</em>，黑客可以在自己的网站上得到这个 <em>token</em>，并马上就可以发动 <em>CSRF</em> 攻击。</p><p>为了避免这一点，系统可以在添加 <em>token</em> 的时候增加一个判断，如果这个链接是链到自己本站的，就在后面添加 <em>token</em>，如果是通向外网则不加。不过，即使这个 <em>csrftoken</em> 不以参数的形式附加在请求之中，黑客的网站也同样可以通过 <em>Referer</em> 来得到这个 <em>token</em> 值以发动 CSRF 攻击。这也是一些用户喜欢手动关闭浏览器 <em>Referer</em> 功能的原因。</p><h2 id="在-http-头中自定义属性并验证" tabindex="-1"><a class="header-anchor" href="#在-http-头中自定义属性并验证" aria-hidden="true">#</a> 在 <em>HTTP</em> 头中自定义属性并验证</h2><p>​ 这种方法也是使用 <em>token</em> 并进行验证，和上一种方法不同的是，这里并不是把 <em>token</em> 以参数的形式置于 <em>HTTP</em> 请求之中，而是把它放到 <em>HTTP</em> 头中自定义的属性里。通过 <em>XMLHttpRequest</em> 这个类，可以一次性给所有该类请求加上 <em>csrftoken</em> 这个 <em>HTTP</em> 头属性，并把 <em>token</em> 值放入其中。这样解决了上种方法在请求中加入 <em>token</em> 的不便，同时，通过 <em>XMLHttpRequest</em> 请求的地址不会被记录到浏览器的地址栏，也不用担心 <em>token</em> 会透过 <em>Referer</em> 泄露到其他网站中去。</p><p>​ 然而这种方法的局限性非常大。<em>XMLHttpRequest</em> 请求通常用于 <em>Ajax</em> 方法中对于页面局部的异步刷新，并非所有的请求都适合用这个类来发起，而且通过该类请求得到的页面不能被浏览器所记录下，从而进行前进，后退，刷新，收藏等操作，给用户带来不便。另外，对于没有进行 <em>CSRF</em> 防护的遗留系统来说，要采用这种方法来进行防护，要把所有请求都改为 <em>XMLHttpRequest</em> 请求，这样几乎是要重写整个网站，这代价无疑是不能接受的。</p>',37),p=[a];function n(c,d){return m(),o("div",null,p)}const l=e(t,[["render",n],["__file","CSRF浅析-9d1fbcae.html.vue"]]);export{l as default};
